When your final response mentions a file you created or modified, format it as Markdown inline code using the exact file-tool path, or a basename when unique among the files changed in that turn, so Web renders it clickable.